# Render Configuration
# This file contains the start command for Render deployment

# Start Command for FREE TIER (use this in Render dashboard):
# gunicorn -w 1 -b 0.0.0.0:$PORT app:app --timeout 300 --worker-class sync --max-requests 100 --max-requests-jitter 10 --log-level info --preload

# Start Command for PAID TIER (if upgraded):
# gunicorn -w 2 -b 0.0.0.0:$PORT app:app --timeout 180 --log-level info
