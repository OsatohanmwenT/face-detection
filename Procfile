# Render Configuration
# This file contains the start command for Render deployment

# Start Command (use this in Render dashboard):
# gunicorn -w 2 -b 0.0.0.0:$PORT app:app --timeout 120 --log-level info

# Alternative with more workers (if you have more resources):
# gunicorn -w 4 -b 0.0.0.0:$PORT app:app --timeout 180 --log-level info
